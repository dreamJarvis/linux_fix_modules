#include <bits/stdc++.h>
using namespace std;

// Driver function
int main(){
    vector<int> in({3, 5, 2, 1, 4});
    vector<int> out({4, 3, 2, 1, 5});

    unordered_map<int, bool> takeover;
    int n = in.size();

    // for(int i = 0; i < n; i++){
    //     takeover[arr[i]] = true;
    // }

    int i = 0, j = 0;
    int count = 0;

    cout << count << endl;
    return 0;
}
