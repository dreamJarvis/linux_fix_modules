#include <bits/stdc++.h>
using namespace std;

// Driver function
int main(){
    // vector<int> arr({3, 2, 5, 1, 4, 6, 10, 4, 2, 5});
    vector<int> a;

    a.push_back(1);
    a.push_back(2);
    a.push_back(3);

    for(auto &i:a)
        cout << i << "\t";
    cout << endl;

    return 0;
}
